
{
  "rules": {
    "jsx-quotes": ["error", "prefer-double"],  // Ensure double quotes are used in JSX
    "qu,
    {
  "extends": ["next/core-web-vitals", "next/typescript"]
}otes": ["error", "single", { "avoidEscape": true }]  // Allow single quotes except to avoid escaping
  }
}